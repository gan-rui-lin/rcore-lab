# QEMU LoongArch 启动调试笔记

## 当前问题

LoongArch 内核编译成功，但 QEMU 启动时陷入异常循环。

### QEMU 日志分析

```
CPU Reset (CPU 0)
 PC=000000001c000000  # CPU 复位后从固件地址开始

loongarch_cpu_do_interrupt: exception: 3 (Page invalid exception for fetch)
 PC 0000000000200020 # 尝试从物理地址 0x200020 执行

exception: 13 (Instruction Non-Existent) # 无限循环异常
```

### 根本原因

1. **链接地址**: 内核链接到虚拟地址 `0x9000000000200000` (DMW1 映射)
2. **ELF Entry Point**: `0x9000000000200000`
3. **QEMU 行为**: 将内核加载到物理地址 0x200000，但尝试从物理地址执行
4. **问题**: DMW 未初始化前，虚拟地址无效

### OSKernel2025 的解决方案

OSKernel2025 使用更高的基地址 `0x9000000090000000`，对应物理地址 0x90000000（约2.25GB）。

可能的原因：
- QEMU 的 virt 机器可能有特殊的内存布局
- 或者使用了不同的启动方式（通过固件/bootloader）

### 尝试的解决方案

#### 方案 1: 调整基地址（当前尝试）
修改链接脚本使用更高的物理地址：
```ld
BASE_ADDRESS = 0x9000000090000000;
```

#### 方案 2: 位置无关启动代码（PIC）
让 `_start` 函数在物理地址可执行：
- 使用相对跳转和加载
- 初始化 DMW 后再跳转到虚拟地址

#### 方案 3: 使用 QEMU 固件
类似 RISC-V 的 RustSBI，可能需要 LoongArch 的 BIOS 固件。

### 文件对比

| 项目 | rcore-lab | OSKernel2025 |
|------|-----------|--------------|
| BASE_ADDRESS | 0x9000000000200000 | 0x9000000090000000 |
| 物理地址 | 0x200000 (2MB) | 0x90000000 (2.25GB) |
| 启动代码 | 相同逻辑 | 相同逻辑 |

### 下一步

1. ✅ 尝试使用 OSKernel2025 的基地址
2. 如果还不行，检查 QEMU 命令行参数是否需要额外选项
3. 研究 LoongArch 的固件启动流程

## 更新记录

- 2026-02-27: 发现地址映射问题，准备测试新的基地址
